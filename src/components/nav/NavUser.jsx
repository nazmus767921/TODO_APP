import { styled } from "styled-components";

const Wrapper = styled.div`
	display: flex;
`;

const NavUser = () => {
	return (
		<Wrapper>Next task is to make a style guide for project typography</Wrapper>
	);
};

export default NavUser;
